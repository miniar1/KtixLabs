'use client';
import React from 'react';
import { useRouter } from 'next/navigation';

const Assembly = () => {

    const router = useRouter();

    return(
    
            <p>Follow the steps below to assemble your robot:</p>
    );
    }
export default Assembly;